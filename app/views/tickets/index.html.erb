<h1>Tickets</h1>
<%= link_to 'New Ticket', new_ticket_path %>
<ul>
  <% @tickets.each do |t| %>
    <li>
      <%= link_to t.subject, t %> - <%= t.status %> - <%= t.company_name %>
    </li>
  <% end %>
</ul>
